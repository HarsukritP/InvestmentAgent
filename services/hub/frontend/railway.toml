[build]
builder = "NIXPACKS"
buildCommand = "npm install && npm run build"

[deploy]
startCommand = "npx serve -s build --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[service]
name = "hub-frontend" 