[phases.setup]
nixPkgs = ["nodejs_18", "npm"]
# No Python packages

[phases.install]
cmds = ["chmod +x setup.sh && ./setup.sh && npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npx serve -s build -l $PORT" 