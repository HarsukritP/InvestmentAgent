# Force npm to run node-gyp with python2 instead of python3
python=/usr/bin/python2

# Set progress to false to avoid excessive logging
progress=false

# Set strict-ssl to false to avoid SSL issues
strict-ssl=false

# Set registry to npm registry
registry=https://registry.npmjs.org/

# Set unsafe-perm to true to avoid permission issues
unsafe-perm=true 