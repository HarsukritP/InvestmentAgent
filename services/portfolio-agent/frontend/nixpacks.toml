[phases.setup]
nixPkgs = ["nodejs_18", "npm"]
nixOverlays = ["https://github.com/railwayapp/nix-npm-overlay/archive/main.tar.gz"]

[phases.install]
cmd = "npm install"

[phases.build]
cmd = "npm run build"

[start]
cmd = "npm run serve" 